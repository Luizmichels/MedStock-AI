[pytest]
pythonpath = .
testpaths = tests
addopts =
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=75
markers =
    lento: testes que treinam modelos de verdade (desmarcar com -m "not lento")
